# Application environment
APP_ENV=development

# Logging
LOG_LEVEL=INFO
LOG_USE_UTC=false

# GenAI API Key (still required for embeddings/chat)
GEN_AI_KEY=your_google_ai_studio_api_key_here

# Database (optional — Docker Compose injects its own values)
# Only needed if connecting outside Docker or for local scripts
# DATABASE_URL format: postgresql://<user>:<password>@<host>:<port>/<db>
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
